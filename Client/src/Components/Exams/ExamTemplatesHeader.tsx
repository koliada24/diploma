import { Button, Container } from "react-bootstrap";

export function ExamTemplatesHeader() {
  return (
    <Container className="d-flex mb-3 p-0">
      <Button className="ms-auto">Add Template</Button>
    </Container>
  );
}